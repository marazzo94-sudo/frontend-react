import styled from 'styled-components';

const Button = styled.button`
  padding: 0.5rem 1rem;
  border: none;
  border-radius: 4px;
  background: #1e90ff;
  color: #fff;
  cursor: pointer;
`;

export default Button;
